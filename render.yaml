services:
  - type: web
    name: bons-voisins
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: moments-conviviaux-db
          property: connectionString

      - key: ADMIN_PIN
        generateValue: true